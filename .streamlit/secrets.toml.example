# Streamlit Cloud 部署配置

## 環境變數設定（可選）

在 Streamlit Cloud 的 Secrets 中可以設定：

```toml
# .streamlit/secrets.toml (不要提交到 Git)

# 如果想使用特定模型，可以設定
MODEL_PATH = "Qwen/Qwen2-0.5B-Instruct"

# 如果有 Hugging Face token (私有模型需要)
# HUGGING_FACE_TOKEN = "hf_xxxxxxxxxxxxx"
```

## 說明

- 應用會自動從 Hugging Face Hub 下載模型
- 首次啟動需要 3-5 分鐘下載模型（約 1GB）
- 模型會被快取，後續啟動會更快
- 使用 Qwen2-0.5B-Instruct 輕量模型以符合 Streamlit Cloud 限制
