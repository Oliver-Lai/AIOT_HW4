# EMNIST Character Recognition - Streamlit Deployment (Optimized)
# Only essential packages for running the web app

# Deep Learning Framework (CPU-only for smaller size)
tensorflow-cpu>=2.16.0,<2.21.0

# Web Framework
streamlit>=1.25.0,<2.0.0

# Interactive Drawing Component
streamlit-drawable-canvas>=0.9.0

# Core Scientific Computing
numpy>=1.23.0,<2.0.0

# Image Processing (headless version, no GUI dependencies)
opencv-python-headless>=4.7.0,<5.0.0
Pillow>=9.0.0,<11.0.0
